import json
import numpy as np
'''
# Create an empty list to store all data in 
all_data = []

# Open the file and store it in the all data list
with open(f"json_files/gesturespersons1-14..json", 'r') as file:
    data = json.load(file)
    for d in data:
        all_data.append(d)


# Open the file and store it in the all data list
with open(f"json_files/gesturesperson20.json", 'r') as file:
    data = json.load(file)
    for d in data:
        all_data.append(d)

# Store the merged data in a new json file
with open('json_files/theNewFile.json', 'w') as json_file:
    json.dump(all_data, json_file, indent=4)
'''